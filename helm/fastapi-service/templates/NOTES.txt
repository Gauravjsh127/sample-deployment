🚀 FastAPI Service has been deployed!

1. Get the application URL by running these commands:
{{- if eq .Values.service.type "LoadBalancer" }}
   NOTE: It may take a few minutes for the LoadBalancer IP to be available.
   You can watch the status by running:
   
   kubectl get svc {{ include "fastapi-service.fullname" . }} -w

   Once the EXTERNAL-IP is available:
   export SERVICE_IP=$(kubectl get svc {{ include "fastapi-service.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
   echo "API URL: http://$SERVICE_IP:{{ .Values.service.port }}"
   echo "Docs URL: http://$SERVICE_IP:{{ .Values.service.port }}/docs"
   echo "Health URL: http://$SERVICE_IP:{{ .Values.service.port }}/health"
{{- else if eq .Values.service.type "ClusterIP" }}
   kubectl port-forward svc/{{ include "fastapi-service.fullname" . }} 8080:{{ .Values.service.port }}
   echo "API URL: http://127.0.0.1:8080"
{{- end }}

2. Check deployment status:
   kubectl get pods -l "app.kubernetes.io/name={{ include "fastapi-service.name" . }}"

3. View logs:
   kubectl logs -l "app.kubernetes.io/name={{ include "fastapi-service.name" . }}" -f

